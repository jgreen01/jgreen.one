---
import { seoMeta, type SeoProps } from '../utils/seoMeta';
import { serializeJsonLd } from '../utils/structuredData';

export type Props = SeoProps;

const { title, description, url, image, type, markdownUrl } = seoMeta(Astro.props);
const { jsonLd } = Astro.props;
---
<title>{title}</title>
<link rel="canonical" href={url} />
{/*
  The Markdown twin. The llms.txt specification recommends advertising it this
  way as well as by the URL convention, and `alternate` is the registered IANA
  relation for an alternate representation of the same resource.
*/}
{markdownUrl && <link rel="alternate" type="text/markdown" href={markdownUrl} />}
<meta name="description" content={description} />

<meta property="og:type" content={type} />
<meta property="og:site_name" content="jgreen.one" />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:url" content={url} />
<meta property="og:image" content={image} />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={image} />

<meta name="theme-color" content="#f6780a" />

{/*
  Structured data. Angle brackets and ampersands are escaped as unicode in
  serializeJsonLd, so a literal </script> inside a value cannot close this
  block early and spill JSON into the document as markup.
*/}
{jsonLd && <script is:inline type="application/ld+json" set:html={serializeJsonLd(jsonLd)} />}
